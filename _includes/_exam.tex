% Shared LaTeX header for exam PDFs.
% Sets PDF metadata, page footer, and a diagonal author watermark.

\usepackage{hyperref}
\hypersetup{
  pdftitle={Exam Example},
  pdfauthor={S. Le Boulanger},
  pdfsubject={EFL — Exam},
  pdfkeywords={EFL, Bildungsplan, Klassenarbeit, Abitur}
}

\usepackage{fancyhdr}
\usepackage{lastpage}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[L]{\small © S. Le Boulanger · EFL}
\fancyfoot[C]{\small \thepage\ / \pageref{LastPage}}
\fancyfoot[R]{\small CC-BY-SA 4.0}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0.3pt}

% Watermark — try draftwatermark, fall back to background, fall back to manual.
\IfFileExists{draftwatermark.sty}{%
  \usepackage{draftwatermark}
  \SetWatermarkText{S. Le Boulanger}
  \SetWatermarkScale{0.6}
  \SetWatermarkLightness{0.92}
  \SetWatermarkAngle{55}
}{%
  \IfFileExists{background.sty}{%
    \usepackage{background}
    \backgroundsetup{
      scale=4,
      angle=55,
      opacity=0.08,
      contents={S. Le Boulanger}
    }
  }{%
    \usepackage{eso-pic}
    \AddToShipoutPictureBG{%
      \AtPageCenter{%
        \makebox(0,0){\rotatebox{55}{\textcolor{black!8}{\fontsize{48pt}{56pt}\selectfont S. Le Boulanger}}}%
      }%
    }%
  }%
}
